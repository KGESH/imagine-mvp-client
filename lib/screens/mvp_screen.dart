import 'package:flutter/material.dart';

class MvpScreen extends StatelessWidget {
  const MvpScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
